import 'package:flutter/material.dart';

class PdfSummaryScreen extends StatelessWidget {
  const PdfSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Pdf_Summary Screen"),
      ),
    );
  }
}